interface:
  display_name: "PSA Strategist"
  short_description: "Design and maintain PSA investor strategies"
  default_prompt: "Use $psa-strategist to build, version, and recall PSA strategies through CLI-only workflow (`psa` command from `psa-strategy-cli`)."
